name: hireflow-resume-screening
version: "1.0.0"
description: "Resume screening environment where an agent learns to evaluate candidates against job descriptions"
spec_version: 1

type: environment

runtime:
  type: docker

app: server.app:app
port: 7860
